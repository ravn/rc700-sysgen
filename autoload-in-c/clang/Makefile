# clang/Makefile — Clean target for clang PROM build artifacts
# Build is handled by parent Makefile (Docker needs parent as workdir).

.PHONY: clean

clean:
	rm -f *.o prom.elf prom.bin prom.lis prom_src.lis banner.h
