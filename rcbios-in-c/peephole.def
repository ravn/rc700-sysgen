	ld	a,0x00
=
	xor	a, a

	ld	a,%1
	out	(%2), a
	ld	a,%1
=
	ld	a,%1
	out	(%2), a

	xor	a, a
	out	(%1), a
	xor	a, a
=
	xor	a, a
	out	(%1), a

	jp	%1
	jp	%2
=
	jp	%1

	jp	%1
	jr	%2
=
	jp	%1

	jr	%1
	jp	%2
=
	jr	%1

	jr	%1
	jr	%2
=
	jr	%1

	ret
	jp	%1
=
	ret

	ret
	jr	%1
=
	ret

