# clang/Makefile — Clean target for clang BIOS build artifacts
# Build is handled by parent Makefile (Docker needs parent as workdir).

.PHONY: clean

clean:
	rm -f *.o bios.elf bios.cim bios.bin bios.lis bios_src.S bios_src.lis
